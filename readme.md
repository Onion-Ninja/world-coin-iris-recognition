This repo has all the codes required to run worldcoin module on the datasets that we have.

wc_pipeline.py: This code reads all the files from the source iris_db dataset and produces the worldcoin outputs for all the images. The databases currently used:

1.
